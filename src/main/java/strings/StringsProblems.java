package main.java.strings;

public class StringsProblems {


  public int findNumberOfPatternOccuranceInGivetText(String pattern, String text) {
    char[] textArray = text.toCharArray();
    char[] patternArray = pattern.toCharArray();

    int counter = 0;

    for (int i = 0; i < textArray.length; i++) {
      boolean previeous = false;
      if (textArray[i] == patternArray[0]) {
        for
      }
    }
    return counter;

  }

}
